# การตั้งค่าระบบ RASME Self-Study v4

## เริ่มใช้งานครั้งแรก

1. เปิดโปรเจกต์ Google Apps Script ที่ผูกกับ Google Sheets
2. รันฟังก์ชัน `initializeSystem()` หนึ่งครั้งและอนุญาตสิทธิ์ที่ระบบร้องขอ
3. สร้างบัญชี Admin จาก Apps Script editor เท่านั้น โดยเรียก `setupAdminAccount_('ชื่อผู้ใช้', 'รหัสผ่านอย่างน้อย 8 ตัวอักษร', 'ชื่อ Admin', 'อีเมล')` ฟังก์ชันลงท้าย `_` เพื่อไม่ให้หน้าเว็บเรียกตั้งบัญชี Admin ได้ ห้ามสร้าง wrapper สาธารณะที่รับรหัสผ่านจากหน้าเว็บ
4. Deploy เป็น Web app โดยให้ทำงานในนามเจ้าของ และอนุญาตผู้ใช้ที่มีลิงก์เข้าถึง

ระบบจะสร้างชีตที่ต้องใช้ทั้งหมดโดยอัตโนมัติ ข้อมูลเดิมจะไม่ถูกลบ

## ชีตที่ Admin ต้องกรอก

### Procedures

- `ProcedureID`: รหัสไม่ซ้ำ เช่น `PROC-CPR`
- `ProcedureName`: ชื่อหัตถการ
- `Description`: รายละเอียดสั้น
- `Location`, `Floor`, `Room`: สถานที่
- `ImageURL`: ลิงก์ภาพปก (เว้นว่างได้)
- `Rules`: กฎเฉพาะหัตถการ คั่นแต่ละข้อด้วย `|`
- `Status`: ใส่ `Active` เพื่อเปิดแสดง
- `SortOrder`: ลำดับการแสดง

### Sessions

- `SessionID`: รหัสรอบไม่ซ้ำ เช่น `SES-20260901-01`
- `ProcedureID`: ต้องตรงกับ Procedures
- `SessionDate`: วันที่จัดรอบ
- `StartTime`, `EndTime`: เวลาเริ่มและสิ้นสุด
- `Room`, `Floor`: เว้นว่างเพื่อใช้ค่าจาก Procedures ได้
- `MinCapacity`, `MaxCapacity`: ค่าเริ่มต้น 2 และ 4
- `BookingOpenDate`: วันที่เริ่มเปิดรับ (เว้นว่างเพื่อเปิดทันที)
- `BookingCloseDays`: ค่าเริ่มต้น 7 วัน
- `Status`: ใส่ `Open` เพื่อเปิดรับ

### Settings

แก้ไขจำนวนวัน ความจุ และ No-show ได้จากชีตนี้โดยไม่ต้องแก้โค้ด ส่วน Telegram ควรเก็บ `TELEGRAM_BOT_TOKEN` และ `TELEGRAM_CHAT_ID` ใน Script Properties

## งานอัตโนมัติ

`initializeSystem()` จะติดตั้งการตรวจทุก 1 นาทีสำหรับสถานะไม่ได้ Check-in การยืนยัน/ยกเลิกรอบเมื่อปิดรับ อีเมลเตือนล่วงหน้า 1 วัน และติดตั้งงานทำความสะอาดข้อมูลเทคนิคทุกคืน ระบบจะตรวจซ้ำทันทีเมื่อผู้เรียนเปิดหน้าการจองของฉัน

ระบบใช้ `ActiveBookings` สำหรับรายการที่ยังดำเนินอยู่ เพื่อลดเวลาค้นหาและป้องกันการจองซ้ำ ส่วน `Bookings` ยังคงเป็นประวัติถาวร ห้ามกรอกหรือย้ายข้อมูลใน `ActiveBookings` ด้วยตนเอง

การแจ้งเตือนจะพักใน `NotificationQueue` และส่งซ้ำอัตโนมัติหากอีเมลหรือ Telegram ขัดข้องชั่วคราว

หลังอัปเดตโค้ดรุ่นนี้ ให้รัน `initializeSystem()` หนึ่งครั้ง เพื่อสร้างชีตใหม่และต่ออายุ trigger โดยไม่ต้องกรอกข้อมูลในชีตใหม่เอง

## การ Check-in

ผู้เรียนเปิดหน้า “การจองของฉัน” และกด “ขอ Check-in” เพื่อดูรหัส Check-in 3 ตัว จากนั้นแจ้งรหัสให้เจ้าหน้าที่ ผู้ดูแลกรอกรหัสในหน้า Admin ตรวจสอบชื่อ รหัสผู้ใช้ หัตถการ วันที่ เวลา และสถานที่ ก่อนกดยืนยัน ระบบจึงบันทึกสถานะ `CheckedIn` เมื่อฝึกเสร็จ ผู้เรียนกด Check-out และทำแบบประเมินก่อนจองครั้งใหม่

ระบบปัจจุบันไม่ใช้ QR Code หรือกล้องสำหรับ Check-in
